version: '3.1'

services:
  myapp:
    container_name: my-app
    image: tiangolo/uvicorn-gunicorn-fastapi:python3.10
    volumes: 
      - /Users/macbook/Desktop/deneme_docker:/app
    ports:
      - 9090:80
